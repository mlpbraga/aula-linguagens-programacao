#include <iostream>

int main() {
    int contador = 1;

    while (contador <= 5) {
        std::cout << "Valor do contador: " << contador << std::endl;
        contador++; // Incrementa o contador a cada iteração
    }

    return 0;
}
